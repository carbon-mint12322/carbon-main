//Need to build this function to validate events and make it more generic later to accomodate other entities as well
 export default function validateEntity(x : any) {
        //pass the value for now along with null errors
        return x;
 }