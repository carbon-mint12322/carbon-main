jsonschema:
  properties:
    personalDetails:
      title: Personal Details
      $ref: '#/definitions/Person'
    reportsTo:
      title: User reports to
      type: string
    roles:
      title: User Roles
      $ref: '#/definitions/UserRoles'
    pin:
      title: Mobile Pin
      type: string
  required:
    - personalDetails
    - roles
uiHints: file:~/specs/jsonschemas/src/forms/ui/add_user.yaml
