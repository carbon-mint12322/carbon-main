export interface VerticalTabComponentViewProps {
    children: JSX.Element,
    title: string,
    cardTitleBarStyle?: Object,
    handleMainBtnClick?: any,
    buttonLabel: string,
    tab?: any,
    jsonSchema?: any,
    uiSchema?: any
    data?: any
}